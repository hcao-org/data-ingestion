{
  "name": "signup-review-app",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "start": "serve public",
    "css": "sass scss/styles.scss public/css/styles.css --no-source-map",
    "css:watch": "sass --watch scss/styles.scss:public/css/styles.css --no-source-map",
    "emulate": "firebase emulators:start --only hosting,functions",
    "deploy": "firebase deploy"
  },
  "devDependencies": {
    "firebase-tools": "^13.15.4",
    "sass": "^1.77.8",
    "serve": "^14.2.3"
  }
}
